# uChurch v17.11.99

This maintenance release pins the patched `qs` 6.16.0 transitive dependency
while keeping the existing Express 4 application contract. It removes three
related moderate production audit findings without changing database formats,
temporary sessions, upload boundaries or the user interface.
