# uChurch v17.11.97

This hotfix repairs live application startup after a Help text regression was
found during exact-tag browser QA. The protected loading screen no longer gets
stuck before the workspace can initialize.

The unlocked landing gallery also has one quiet visibility control. Photos fade
out and return smoothly, and the browser remembers the visitor's choice without
storing CRM or account data.
