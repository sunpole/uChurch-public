# uChurch v17.11.79

## Русский

Зафиксирован внутренний synthetic-only этап WEB6-I. Повторно проверены границы
зашифрованного пакета, временной сессии, очистки workspace, ограничений
приглашений, origin-политики и журналов без содержимого базы.

Это не внешний security audit и не включение пользовательских баз. Постоянная
среда `staging.uchurch.ru` остаётся синтетической Demo. До последующих
контролируемых шагов реальные базы, пароли и recovery-файлы не принимаются.

## English

An internal synthetic-only WEB6-I checkpoint was recorded. Package, temporary
session, cleanup, invitation, origin, and data-free logging boundaries were
rechecked. This is not an external security audit and does not enable user
databases.
