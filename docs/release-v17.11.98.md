# uChurch v17.11.98

The synthetic Demo now enters its workspace directly with the single fixed
Demo operator. The visitor no longer stops at a one-choice operator screen.

Explicit session ending first removes the temporary server workspace and then
returns to `uchurch.ru`. The retired database and operator entry screen is no
longer shown after leaving Demo. Owner database operator selection remains
unchanged.
